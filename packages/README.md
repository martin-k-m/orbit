# packages/

Reserved for shared, reusable packages consumed by more than one crate or app
in this repo — for example a shared TypeScript types package generated from the
Rust model and consumed by the desktop frontend (`apps/desktop/ui`).

Nothing lives here yet; it exists so the structure is ready to grow without
another reorganisation.
